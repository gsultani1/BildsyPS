# BildsyPS — Status Update
**Date:** 2026-02-20  
**Version:** 1.3.0  
**Test coverage:** 313 tests, 0 failures, 1 skipped

---

## Sessions completed today (3)

### Session 1 — E2E Audit & Bug Fixes
Full codebase audit. 11 defects found and fixed:

| # | Defect | Fix |
|---|--------|-----|
| 1 | `OrderedDictionary.ContainsKey()` — 10 sites | Changed to `.Contains()` in `PluginLoader.ps1` (7) and `UserSkills.ps1` (3) |
| 2 | `Get-Content` char coercion — 3 sites | Wrapped with `@()` in `SecretScanner.ps1`, `ConfigLoader.ps1`, `PersistentAliases.ps1` |
| 3 | Version mismatch — profile 1.2.0 vs module 1.3.0 | Fixed profile |
| 4 | `SecretScanner` single finding unwrapped as bare hashtable | `return @(,$findings)` |
| 5 | `ResponseParser` null `ChatSessionHistory` crash on second `+=` | Added null guard |
| 6 | `ProfileTimings` null crash in `PluginLoader.ps1` | Added init in bootstrap |
| 7 | `ChatStorage` DB init crash — `$global:ChatLogsPath` null at load time | Added default init in `ChatStorage.ps1` line 7 |
| 8 | `UserSkills` not shell-invocable — registered as intents only | `Set-Item function:$skillName` per skill; `Remove-Item` on unregister |
| 9 | `UserSkills.json` not auto-created on first run | `Import-UserSkills` now copies `UserSkills.example.json` if missing |
| 10 | `Get-AppBuilds` returns `$null` — writes to host but never returns array | Added `return $builds`; fixed `$($b.BuildTime)` interpolation |
| 11 | `AppBuilder` missing dangerous code patterns | Added `os.system`, `subprocess`, `Remove-Item -Recurse`, `Start-Process -Verb RunAs` to `Test-GeneratedCode` |

**New features added:**
- `Invoke-UserSkill` — public function to execute skills by name with `{param}` substitution
- Trigger phrase registration — `Import-UserSkills` registers `triggers` array into `IntentAliases`; cleanup in `Unregister-UserSkills` and `Remove-UserSkill`

**Test coverage:** 112 → 276 tests (0 failures, 3 skipped)

---

### Session 2 — Project Rename: Shelix → BildsyPS
Full codebase rename across 63 files.

| Item | Old | New |
|------|-----|-----|
| Display name | Shelix | BildsyPS |
| Global variables | `$global:ShelixHome` | `$global:BildsyPSHome` |
| User data dir | `~/.shelix` | `~/.bildsyps` |
| Module files | `Shelix.psm1`, `Shelix.psd1` | `BildsyPS.psm1`, `BildsyPS.psd1` |
| Functions | `Initialize-ShelixHome`, etc. | `Initialize-BildsyPSHome`, etc. |

276 tests pass after rename. Only remaining "Shelix" references are in `.claude/worktrees/` (not project code).

---

### Session 3 — Tab Completion Overhaul

#### Audit findings
Existing completers (15 total) covered: `IntentRouter.ps1`, `PluginLoader.ps1`, `AgentTools.ps1`, `UserSkills.ps1`.

Missing completers identified across 10 modules.

#### Completers added (22 new)

| Module | Parameter | Source |
|--------|-----------|--------|
| `ChatProviders.ps1` | `Set-DefaultChatProvider -Provider` | `$global:ChatProviders.Keys` |
| `ChatProviders.ps1` | `Get-ChatModels -Provider` | `$global:ChatProviders.Keys` |
| `ChatProviders.ps1` | `Test-ChatProvider -Provider` | `$global:ChatProviders.Keys` |
| `ChatSession.ps1` | `Start-ChatSession -Provider` | `$global:ChatProviders.Keys` |
| `ChatSession.ps1` | `chat -Provider` | `$global:ChatProviders.Keys` |
| `ChatSession.ps1` | `Resume-Chat -Name` | SQLite sessions → JSON index fallback |
| `ChatSession.ps1` | `Remove-ChatSession -Name` | SQLite sessions → JSON index fallback |
| `ChatSession.ps1` | `Export-ChatSession -Name` | SQLite sessions → JSON index fallback |
| `UserSkills.ps1` | `Invoke-UserSkill -Name` | `$global:LoadedUserSkills.Keys` |
| `WorkflowEngine.ps1` | `Invoke-Workflow -Name` | `$global:Workflows.Keys` (tooltip = description) |
| `MCPClient.ps1` | `Connect-MCPServer -Name` | `$global:MCPServers.Keys` (registered) |
| `MCPClient.ps1` | `Disconnect-MCPServer -Name` | `$global:MCPConnections.Keys` (connected) |
| `MCPClient.ps1` | `Get-MCPTools -Name` | `$global:MCPConnections.Keys` |
| `MCPClient.ps1` | `Invoke-MCPTool -ServerName` | `$global:MCPConnections.Keys` |
| `AppBuilder.ps1` | `Remove-AppBuild -Name` | Subdirs of `$global:AppBuilderPath` |
| `AppBuilder.ps1` | `Update-AppBuild -Name` | Subdirs of `$global:AppBuilderPath` |
| `AppBuilder.ps1` | `New-AppBuild -Framework` | Static: `powershell`, `python-tk`, `python-web` |
| `AgentHeartbeat.ps1` | `Remove-AgentTask -Id` | `Get-AgentTaskList` (tooltip = task description) |
| `AgentHeartbeat.ps1` | `Enable-AgentTask -Id` | `Get-AgentTaskList` |
| `AgentHeartbeat.ps1` | `Disable-AgentTask -Id` | `Get-AgentTaskList` |
| `PersistentAliases.ps1` | `Remove-PersistentAlias -Name` | Parse `Set-Alias` lines from `$global:UserAliasesPath` |
| `CodeArtifacts.ps1` | `Remove-Artifact -Name` | Files in `$global:ArtifactsPath` |
| `NavigationUtils.ps1` | `gco -Branch` | `git branch --list` |
| `NavigationUtils.ps1` | `gmerge -Branch` | `git branch --list` |
| `NavigationUtils.ps1` | `grb -Branch` | `git branch --list` |

#### Bug fix: `gm` → `gmerge`
`gm` in `NavigationUtils.ps1` conflicted with PowerShell's built-in `gm` alias for `Get-Member`, preventing the tab completer from firing. Renamed to `gmerge`.

#### Tests added
`Tests/TabCompletion.Tests.ps1` — 37 tests covering all new and pre-existing completers.  
**Result: 37 passed, 0 failed, 0 skipped.**

**Total test count: 276 → 313**

---

## Current state

### Modules (37 total)
All modules load cleanly. No known defects.

| Module | Status |
|--------|--------|
| `ConfigLoader.ps1` | ✅ |
| `PlatformUtils.ps1` | ✅ |
| `SecurityUtils.ps1` | ✅ |
| `SecretScanner.ps1` | ✅ |
| `CommandValidation.ps1` | ✅ |
| `SafetySystem.ps1` | ✅ |
| `NaturalLanguage.ps1` | ✅ |
| `ResponseParser.ps1` | ✅ |
| `CodeArtifacts.ps1` | ✅ |
| `VisionTools.ps1` | ✅ |
| `OCRTools.ps1` | ✅ |
| `ChatStorage.ps1` | ✅ |
| `ChatProviders.ps1` | ✅ |
| `ChatSession.ps1` | ✅ |
| `AppBuilder.ps1` | ✅ |
| `AgentHeartbeat.ps1` | ✅ |
| `AgentTools.ps1` | ✅ |
| `AgentLoop.ps1` | ✅ |
| `IntentAliasSystem.ps1` | ✅ |
| `IntentRegistry.ps1` | ✅ |
| `IntentActions.ps1` | ✅ |
| `IntentActionsSystem.ps1` | ✅ |
| `WorkflowEngine.ps1` | ✅ |
| `IntentRouter.ps1` | ✅ |
| `UserSkills.ps1` | ✅ |
| `PluginLoader.ps1` | ✅ |
| `MCPClient.ps1` | ✅ |
| `BrowserAwareness.ps1` | ✅ |
| `FolderContext.ps1` | ✅ |
| `NavigationUtils.ps1` | ✅ |
| `PersistentAliases.ps1` | ✅ |
| `SystemUtilities.ps1` | ✅ |
| `ArchiveUtils.ps1` | ✅ |
| `DockerTools.ps1` | ✅ |
| `DevTools.ps1` | ✅ |
| `TerminalTools.ps1` | ✅ |
| `PackageManager.ps1` | ✅ |
| `WebTools.ps1` | ✅ |
| `ProductivityTools.ps1` | ✅ |
| `DocumentTools.ps1` | ✅ |
| `FzfIntegration.ps1` | ✅ |
| `ProfileHelp.ps1` | ✅ |
| `ToastNotifications.ps1` | ✅ |

### Test files (16 total)

| File | Tests |
|------|-------|
| `AgentHeartbeat.Tests.ps1` | — |
| `AgentLoop.Tests.ps1` | — |
| `AppBuilder.Tests.ps1` | — |
| `ChatStorage.Tests.ps1` | — |
| `CodeArtifacts.Tests.ps1` | — |
| `ConfigLoader.Tests.ps1` | — |
| `IntentRouting.Tests.ps1` | — |
| `NaturalLanguage.Tests.ps1` | — |
| `PluginLoader.Tests.ps1` | — |
| `ResponseParser.Tests.ps1` | — |
| `SafetySystem.Tests.ps1` | — |
| `SecretScanner.Tests.ps1` | — |
| `TabCompletion.Tests.ps1` | 37 (new today) |
| `UserSkills.Tests.ps1` | — |
| `VisionTools.Tests.ps1` | — |
| `WorkflowEngine.Tests.ps1` | — |
| **Total** | **313** |

---

## Roadmap — next priorities

| Priority | Item |
|----------|------|
| 🔜 High | Browser automation — Selenium WebDriver integration |
| 🔜 High | Remote listener + webhooks — receive commands via Twilio/HTTP |
| 🔜 Medium | GUI layer — mission control dashboard (WPF/Avalonia) |
| 🔜 Medium | RAG over conversation history — vector search on SQLite sessions |
| 🔜 Low | App Builder templates — pre-built scaffolds for common app types |
| 🔜 Low | Cross-platform testing — macOS/Linux via PowerShell 7 |
